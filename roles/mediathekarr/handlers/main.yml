---
- name: Apply netplan
  ansible.builtin.command:
    cmd: netplan apply
  changed_when: true

- name: Restart mediathekarr
  community.docker.docker_compose_v2:
    project_src: "{{ mediathekarr_data_dir }}"
    state: restarted
