# {{ ansible_managed }}
[{{ nut_ups_name }}]
    driver = {{ nut_ups_driver }}
    port = {{ nut_ups_port }}
    desc = "{{ nut_ups_desc }}"
    override.battery.charge.low = {{ nut_battery_low_charge }}
    override.battery.runtime.low = {{ nut_battery_low_runtime }}
